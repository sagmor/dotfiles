--skip-bundle #skips bundle install on generating a new rails app
--skip-test-unit #skips making the test unit folders (I use rspec)
--database=postgresql #defaults my development database to PostgreSQL instead of SQLite3
--template=~/.dotfiles/ruby/rails-template.rb
